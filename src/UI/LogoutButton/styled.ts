import styled from "styled-components";

export const LogoutButtonWrapp = styled.button``;

const size = 18;
export const LogoutButtonIcon = styled.img`
  display: inline-block;
  height: ${size}px;
  width: ${size}px;
`;
