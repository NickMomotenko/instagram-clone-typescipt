import React from "react";

import { useInput } from "./useInput";

export const useDisplayedInputs = () => {
//   const fullname = useInput(user["fullname"], "fullname");

//   return [fullname];
};
